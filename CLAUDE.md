# CLAUDE.md

このプロジェクトは、小学2年生（1学期相当）向けの算数・国語 学習ドリルゲームです。
iPadのSafariでURLを開くだけで遊べるWebアプリとして作ります。

- 公開URL（Wi-Fi外からもアクセス可）: https://udongirai.github.io/claude-code/
- リポジトリ: https://github.com/udongirai/claude-code （Public。GitHub Pages無料利用のため公開設定にしている）

## ゲームジャンル・コンセプト

- 対象: 小学2年生・1学期相当の学習内容
- ジャンル: 教科ドリル形式の学習ゲーム（多肢選択／数字入力クイズ）
- 収録単元
  - さんすう: 九九（暗記用の単語カードモード＋クイズ）、繰り上がり・繰り下がりのある足し算/引き算
  - こくご: 2年生で習う漢字の読み書き（158字収録、予習用）、
    書き順のれんしゅう（ランダムに出題された漢字を、薄いガイド線から正しい書き順どおりにタップしていくテスト形式。
    練習したい漢字がある場合は画面内のボタンから158字の一覧を選んで指定することもできる）、文章の読解
- 見た目はヒーロー・バトル系（ダーク＋赤/青/金のアクセント）
- 正解するたびに敵モンスターのHPが減り、問題を解き終えると撃破演出が出る
- 3連続正解でコンボ演出（効果音・シェイクアニメーション）
- 各クイズ単元を満点クリアすると称号（例: くくマスター）を獲得でき、「しょうごうずかん」で一覧できる
- 正解時は決めゼリフ（テキスト）をランダム表示し、音声読み上げ機能自体は無し。フィードバックは効果音とアニメーション中心
- オフライン対応は不要（Wi-Fi環境での利用を想定）

## 使用技術

- HTML / CSS / JavaScript（素のJS、フレームワーク・ビルドツール不使用）
- `<script type="module">` は使わず、`<script>` タグの読み込み順で依存関係を解決する
  （file:// で直接開いても動作確認できるようにするため）
- 効果音は音声ファイルを使わず、Web Audio API で都度合成する
- スコア・進捗は `localStorage` に保存（サーバー不要）
- ホスティングは GitHub Pages（このリポジトリの `main` ブランチ）
- 漢字の筆順データは KanjiVG（https://kanjivg.tagaini.net/、CC BY-SA 3.0）から取得したストロークパスを
  `js/data-kanji-strokes.js` に埋め込んで利用。画面上にクレジット表記が必須
- 筆順アニメーションは inline SVG を使うため、file:// で直接開くフローとは別に、
  ローカルサーバー経由での確認を基本とする（データ自体はスクリプトタグ読み込みなのでfile://でも動作はする）

## ディレクトリ構成

```
index.html          # 画面遷移を持つ単一ページ
css/style.css        # 見た目（タップ操作前提の大きいUI）
js/app.js            # 画面状態管理（タイトル→科目→単元→クイズ→結果）
js/quiz.js           # クイズ共通ロジック（4択形式／数字入力形式）
js/sound.js          # Web Audio APIによる効果音（正解/不正解/コンボ/称号獲得など）
js/data-kuku.js       # 九九クイズの問題データ生成
js/data-kuku-yomi.js  # 九九の伝統的な読み（単語カードモード用）
js/data-keisan.js     # 繰り上がり・繰り下がり計算の問題データ生成
js/data-kanji.js      # 2年生漢字（158字）の読み書き問題データ
js/data-kanji-strokes.js # KanjiVG由来の筆順ストロークパスデータ
js/stroke-practice.js # 「書き順のれんしゅう」用：薄い字をタップして書き順を当てるゲーム（ランダム出題＋漢字指定選択の両対応）
js/data-dokkai.js      # 文章読解の問題データ
```

## ビルド・実行・テストコマンド

- ビルド不要（静的ファイルのみ）
- ローカル確認: プロジェクトルートで `python3 -m http.server 8000` を実行し、
  iPadのSafariから `http://<MacのIPアドレス>:8000` を開く
- 自動テストは無し。変更後は実機のSafariで一通り操作して確認する
- 公開: `main` ブランチにpushするとGitHub Pagesに反映される

## コーディング規約

- 外部ライブラリ・フレームワークは使わない（学習用途でもシンプルさを優先）
- 問題データは `js/data-*.js` に集約し、UIロジックと分離する
- ボタン等のタップ領域は最低44px四方を確保する
- 説明文はひらがな中心にし、漢字を使う場合はふりがな（`<ruby>`）を付ける

## 既知の注意点

- `js/data-kanji.js` の158字は担当者（Claude）の記憶を頼りに再構成した2年生配当漢字リストで、
  文部科学省の正式な学年別漢字配当表（160字）と完全一致する保証はない（2字程度の過不足の可能性）。
  実際の学校配布の漢字一覧と照らし合わせて確認するのが望ましい

## 作業時の注意

- 技術選定について相談があれば、ゲーム内容に応じて提案する
